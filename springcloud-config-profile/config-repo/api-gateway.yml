management:
  endpoints:
    web:
      exposure:
        include: "*"
zuul:
  routes:
    hello-service:
      path: /**
    api-feign:
      path: /feign-api/**
      serviceId: feign-consumer
  add-host-header: true