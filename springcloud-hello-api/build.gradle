plugins {
    id 'application'
}

group 'com.springcloud.demo'
version "${parent.version}"
archivesBaseName = 'hello-api'

dependencies {
    compile group: 'org.springframework.boot', name: 'spring-boot-starter-web'
}
