spring:
  application:
    name: api-gateway
  cloud:
    config:
#      uri: http://localhost:7070
      username: user
      password: 3f3a0a7a-03e9-4cd6-9893-42484b81604e
      discovery:
        enabled: true
        service-id: config-server
      fail-fast: true
      retry:
        max-attempts: 10
  rabbitmq:
    port: 5672
    host: localhost
    username: springcloud
    password: 123456
server:
  port: 8801
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8010/eureka,http://localhost:8020/eureka
