spring:
  application:
    name: api-gateway
  cloud:
    config:
      uri: http://localhost:7070/
      label: master
server:
  port: 8801
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8010/eureka,http://localhost:8020/eureka
